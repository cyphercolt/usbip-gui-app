"""Node-to-node trust: pairing token + optional web PIN.

Phase 0: stub. Phase 2 implements first-run pairing (short code, no SSH passwords) and gates the
command API so only paired nodes / an authenticated browser can drive USB.
"""
